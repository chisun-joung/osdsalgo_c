#include <stdio.h>

int main()
{
	int item[4] = {0,};
	int i;

	item[0] = 1;
	item[3] = 1;

	for(i=0; i<4; i++ )
		if( item[i] )
			printf("%d\n",  i );
	printf("----------\n");
	
	item[3] = 0;
	for(i=0; i<4; i++ )
		if( item[i] )
			printf("%d\n",  i );

	return 0;
}
